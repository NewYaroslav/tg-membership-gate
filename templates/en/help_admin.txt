Admin commands:
/ban <ID> — ban a user
/unban <ID> — unban a user
/kick <ID> — remove from channels
/remove <ID> — alias of /kick
/export_users [all|confirmed|unconfirmed|banned] — export user list
/user <ID> — show user info
