<b>📖 Справка для администраторов</b>

Доступные команды:

/start — начать обращение  
/help — показать справку  

<b>🔧 Админ-команды:</b>
/add_users &lt;email&gt; — добавить или разблокировать email
/remove_users &lt;email&gt; — удалить email из базы
/check_user &lt;email&gt; — проверить статус email
/myid — узнать Telegram ID чата и пользователя

📎 Также вы можете отправить CSV-файл со списком email-адресов после команды <b>/add_users</b>. Каждая строка должна содержать один email. Бот обработает их автоматически.

💡 Можно указывать несколько адресов через пробел или использовать CSV-файл для пакетной обработки.

🔁 Для удобства доступны синонимы команд: <code>/add_email</code>, <code>/remove_email</code>, <code>/check_email</code>.

🚦 <b>Ограничения:</b>  
Для защиты от спама действует лимит — не более {{ max_requests }} обращений за {{ interval_minutes }} минут. Если лимит превышен, бот временно заблокирует приём новых обращений.